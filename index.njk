---
layout: base.njk
title: Home
---

<div class="hero">
    <h1>Welcome to My Site</h1>
    <p class="lead">A place for thoughts, ideas, and discoveries.</p>
</div>

<section class="recent-posts">
    <h2>Recent Posts</h2>
    <div class="post-list">
        {% for post in collections.posts | reverse | slice(0, 3) %}
        <article class="post-card">
            <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
            <div class="post-meta">
                {{ post.date | date }}
            </div>
            <p>{{ post.data.description or post.templateContent | safe | truncate(150) }}</p>
            <a href="{{ post.url }}" class="read-more">Read more →</a>
        </article>
        {% endfor %}
    </div>
</section>

<style>
.hero {
    text-align: center;
    padding: 4rem 0;
    background: var(--secondary-background);
    border-radius: 0.5rem;
    margin-bottom: 3rem;
}

.hero h1 {
    font-size: 3rem;
    margin-bottom: 1rem;
}

.lead {
    font-size: 1.25rem;
    color: #4b5563;
}

.read-more {
    display: inline-block;
    margin-top: 1rem;
    font-weight: 500;
}
</style>
